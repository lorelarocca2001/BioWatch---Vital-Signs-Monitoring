<source>
  @type http
  port 9880            # La porta su cui Fluentd ascolta (9880 è quella usata nel tuo script)
  bind 0.0.0.0         # Permette di accettare connessioni da tutte le interfacce di rete
</source>

#<match patient.vitals>
  #@type stdout
  #format json          # I dati saranno visualizzati in formato JSON
#</match>

<match patient.vitals>
   @type kafka2

   # list of seed brokers
   brokers broker:9092

   # topic settings
   topic vitaldb

   <format>
     @type json
   </format>
 </match>